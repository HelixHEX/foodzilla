import { StyleSheet, Platform } from "react-native"

export const styles = StyleSheet.create({
    title: {
        color: '#1f1f1f',
        fontSize: 25,
        fontWeight: '200',
        marginTop: '5%',
        marginLeft: '5%'
    }
})